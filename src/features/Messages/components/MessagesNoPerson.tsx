/** Displays a message informing the user that there is no person selected */
const MessagesNoPerson = () => {
  return (
    <div className="h-full w-full text-lg text-gray-800 font-semibold flex items-center justify-center">
      <p>Select a person to start a conversation.</p>
    </div>
  );
};

export default MessagesNoPerson;
